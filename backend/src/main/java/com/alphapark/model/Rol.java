package com.alphapark.model;

/**
 * Refleja exactamente los valores del ENUM('Empleado','Gerente')
 * de la columna operadores.rol en la base de datos.
 */
public enum Rol {
    Empleado,
    Gerente
}
